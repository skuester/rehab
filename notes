_default.html.rule




- executable template (bit)

	#!inmate splyce



core abstraction:
	[:static, 'text']
	[:dynamic, '@person.name']
	[:code, 'if @person == foobar']


string > parser#call > Sexp

Sexp > filter#call > Sexp

Sexp > generator#call > string (valid ruby)

Sexp > compiler#call > string (out)


Engine
	parsers
	filters
	generator
